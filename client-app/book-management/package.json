{
  "name": "book-management",
  "version": "1.0.0",
  "description": "electron book mangement app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "copyfiles -u 1 src/**/*.html dist/",
    "build": "tsc",
    "start": "npm run build && electron --inspect=9222 ./dist/electron/main.js"
  },
  "author": "boong",
  "license": "ISC",
  "devDependencies": {
    "@types/jquery": "^3.5.29",
    "copyfiles": "^2.4.1",
    "electron": "^28.0.0"
  },
  "dependencies": {
    "jquery": "^3.7.1",
    "typescript": "^5.3.3"
  }
}
